#include <stdio.h>


int main()
{
    printf("Hello world\n");

    int two = 2, three = 3, six = 6;
    int result = (two + three) * (six / three);

    printf("The result of the expression is: %d\n", result);

    return 0;
}